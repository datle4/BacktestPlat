spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:h2:mem:backtest;MODE=PostgreSQL;DB_CLOSE_DELAY=-1}
    username: ${TEST_DB_USER:sa}
    password: ${TEST_DB_PASSWORD:}
  flyway:
    enabled: true
app:
  market:
    scheduled: false
