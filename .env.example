# Local development only. PostgreSQL runs in Docker.
DB_USER=backtestplat
DB_PASSWORD=local-backtestplat
DB_PORT=55432
DB_URL=jdbc:postgresql://localhost:55432/backtestplat

# Optional full application stack (docker compose --profile app up).
# Separate ports keep IntelliJ :8080 and Vite :5173 available.
BACKEND_PORT=8081
FRONTEND_PORT=8088
